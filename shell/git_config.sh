#!/bin/bash 
# This just configs some nice global git stuffs

git config --global user.name "Deena Rennerfeldt"
git config --global user.email "deena@deenasdomain.com"
git config --global color.ui auto
git config --global alias.st status
git config --global alias.co checkout
git config --global push.default simple
